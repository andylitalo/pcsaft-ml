name: CI

on:
  push:
    branches:
      - main
      - "**"
  pull_request:
  workflow_dispatch:

jobs:
  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Set up uv
        uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true

      - name: Sync development dependencies
        run: uv sync --extra dev

      - name: Lint with ruff
        run: uv run ruff check .

  test-core:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Set up uv
        uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true

      - name: Sync development dependencies
        run: uv sync --extra dev

      - name: Run core tests (exclude slow, skip optional-dep tests)
        run: >
          uv run pytest tests/ -v -m "not slow" --tb=short
          --ignore=tests/test_chemberta.py
          --ignore=tests/test_chemberta_ad.py
          --ignore=tests/test_henrys_constant.py
          --ignore=tests/test_hfo_screening.py
          --ignore=tests/test_kubeflow.py
          --ignore=tests/test_nist_validation.py
          --ignore=tests/test_portal.py
          --ignore=tests/test_portal_presets.py
          --ignore=tests/test_reference_molecules.py
          --ignore=tests/test_serving.py
          --ignore=tests/test_similarity.py
          --ignore=tests/test_temp_sweep.py
          --ignore=tests/test_thermodynamic.py
          --ignore=tests/test_verify_candidates.py

  test-package:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install pcsaft-predict package
        run: pip install -e "./pcsaft_predict[dev]"

      - name: Run package tests
        run: pytest tests/test_pcsaft_predict.py -v --tb=short

  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install build tools
        run: pip install build

      - name: Build package
        run: python -m build pcsaft_predict/

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: pcsaft_predict/dist/
